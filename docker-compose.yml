version: '2.2'
services:
  php-apache:
    container_name: php-apache
    image: php:8.1-apache
    volumes:
      - ./app:/var/www/html/
    networks:
      - backend
    ports:
      - "8000:80"
networks:
  backend: