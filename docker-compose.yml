version: '2.2'
services:
  php-apache:
    container_name: php-apache
    build: ./docker/php-apache
    volumes:
      - ./app:/var/www/html/
    networks:
      - backend
    ports:
      #- "80:80"   #127.0.0.1
      #- "8080:80" #127.0.0.1:8080
      #- "8000:80" #127.0.0.1:8000
      - "80:80"
networks:
  backend: