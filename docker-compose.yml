version: '2.2'
services:
  php-apache:
    container_name: php-apache
    build: ./docker/php-apache
    volumes:
      - ./app:/var/www/html/
    networks:
      - backend
    ports:
      - "8000:80"
networks:
  backend: